This is a dummy backend service designed for use in Octoparts integration tests and manual troubleshooting.

It's a Play app that runs on port 9001.

```
cd test-backend
sbt run
```

should be enough to start the server.

